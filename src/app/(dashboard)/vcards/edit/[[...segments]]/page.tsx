/**
 * The editor UI is rendered by `../layout.tsx` so section URLs never remount it.
 * This page only exists so `/vcards/edit/*` resolves.
 */
export default function VCardEditSectionPage() {
  return null
}
